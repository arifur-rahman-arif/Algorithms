let strs = ["flower", "flow", "flight"];
/**
 * @method #1
 * @Time_Complexity : O(n)
 * @Space_Complexity : O(1)
 */
const longestCommonPrefix = function (strs) {
    if (strs == undefined || 0 == strs.length) {
        return '';
    }
    for (let i = 0; i < strs.length; i++) {

    }
};